# Independent QA checkpoint — 13 September 2026

Scope: current React working tree and freshly built `frontend/dist`; no application edits, production deploy, account creation or live user-data writes.

| Check | Result | Evidence / limitation |
| --- | --- | --- |
| Consolidated automated suite | PASS | 18 test cases; library compatibility checks; full original-baseline calendar RLS and original access/points tests; unchanged original RPC definitions; preserved artifact hashes; two-file SQL sanity scan |
| Production build | PASS | TypeScript and Vite 6.4.3; 2,049 modules; initial JS 538.52 KB / 159.56 KB gzip |
| Original artifact preservation | PASS | Build-time SHA-256 and inline-script CSP checks; original app/auth/cloud artifacts retained |
| Built public routes | PASS | Campaign-query root redirects to supplied landing; sign-in, signup, protected deep links and unauthenticated recovery render at 390/1366 px; no horizontal overflow or page runtime errors |
| Built signed-in routes | PASS with fixtures | Today, tasks, saves, calendar, progress, journal, people and profile at 390/1366 px using synthetic empty account; no alerts, horizontal overflow, runtime errors or mutation requests |
| Preserved original app | PASS with fixtures | Original regression run against React dist: auth, task save, recurrence, fair points, account/week leaderboard, opt-out, calendar, six original views, save-before-logout; no runtime errors |
| Content security policy | PASS | Local harness and replacement Netlify draft headers verified; pinned MSAL host is narrowly allowed |
| Real email/Google/reset round trip | PARTIAL PASS | Google OAuth completed and returned to the replacement draft; email/password and password-reset delivery were not repeated |
| Netlify draft smoke | PASS | Replacement draft routes, headers, assets, MSAL response/SRI/browser execution and auth gates pass |
| Populated React cross-device/photo parity | PARTIAL PASS | Existing-account Today, tasks, saves, calendar, progress, journal, people and profile loaded read-only; photo writes and second-device synchronization remain pending |

All Supabase traffic in the isolated browser harnesses was intercepted and fulfilled locally. Mocked login is not evidence that the configured email/Google providers work. Live database checks reported by the parent are separate evidence, not independently rerun here.

Initial sandbox runs failed before completing: tsx `uv_os_get_passwd` returned ENOMEM, and esbuild could not traverse a parent directory. Re-running the same authorized commands with sandbox escalation passed. These were environment failures, not application assertion failures.

Non-blocking build warnings remain: initial chunk exceeds 500 KB and mixed static/dynamic Supabase imports do not produce a separate chunk.

## Immutable draft smoke

The first draft exposed a removed Microsoft CDN asset. The replacement draft `https://6aa70f839e2cb9d6d6a0a98b--cadens-private-beta.netlify.app` uses the same existing Netlify site and was retested without creating records or changing user data.

- PASS: `/` and campaign-query root reach `/welcome.html`; `/signin` and `/signup` render their respective auth views.
- PASS: unauthenticated `/calendar`, `/tasks` and `/saves` remain gated by login. A raw `/?recovery=1` marker reaches the landing and cannot activate password update without a recovery session.
- PASS: preserved `/app.html` hands off to `/cadens-auth.html#signin` without a session.
- PASS: 390 px and 1366 px layouts have no horizontal overflow; no page runtime errors across these routes.
- PASS: main HTML routes return 200 with CSP, `nosniff`, `DENY` and `no-cache`; hashed entry JS/CSS return 200 with immutable caching; preserved cloud/auth JS and cloud CSS return 200 with no-cache.
- PASS: the repaired MSAL 2.38.3 asset returns 200, its SHA-384 matches the deployed integrity attribute, the CSP allows only its pinned host, and headless Chromium exposes `PublicClientApplication` without page or request failures.
- PASS: real Google OAuth returned to the replacement draft and opened the React Today dashboard for the existing account.
- PASS: existing-account Today, tasks, saves, calendar, progress, journal, people, profile and the preserved `/app.html` route loaded read-only without visible error alerts.

## Production smoke

Production deploy `6aa7502dc5c59adc42dc1fae` passed the repeatable route, MSAL response, SHA-384, CSP and browser-execution smoke test. A signed-out clean origin reached the React landing page; the existing signed-in session reached React Today with the preserved original-tools link available.
